import React from "react";
import { createRoot } from "react-dom/client";
import Future from "./Future";
import "./future.css";

createRoot(document.getElementById("root")!).render(<React.StrictMode><Future /></React.StrictMode>);
